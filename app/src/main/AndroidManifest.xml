<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.edu.stu.thigkbanxemay">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="vn.edu.stu.Util.GlobalApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_xemay"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_xemay"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name="vn.edu.stu.thigkbanxemay.AboutActivity" />
        <activity android:name="vn.edu.stu.thigkbanxemay.ChiTietXeMayActivity" />
        <activity android:name="vn.edu.stu.thigkbanxemay.PhanLoaiActivity" />
        <activity android:name="vn.edu.stu.thigkbanxemay.QuanLyPhanLoaiActivity" />
        <activity android:name="vn.edu.stu.thigkbanxemay.ThemSuaXeMayActivity" />
        <activity android:name="vn.edu.stu.thigkbanxemay.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="vn.edu.stu.thigkbanxemay.MainActivity">

        </activity>
    </application>

</manifest>